{
  "name": "yondler1",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd apps/web && npm ci && npm run build",
    "dev": "cd apps/web && npm run dev",
    "start": "cd apps/web && npm run start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
